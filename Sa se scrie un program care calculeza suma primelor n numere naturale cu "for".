#include<stdio.h>
void main()
{
 	int n,s=0; /* cerem sistemului un numar intreg n si unul deja egal cu 0 */
 	printf("Introduceti n:"); /* cerem operatorului un numar intreg n */
 	scanf("%d",&n); /* citim numarul introdus de operator si il definim ca n */
 	for(int i=1;i<=n;i++) /* pentru i=1 pana la i=n adaugam o unitate lui i pentru fiecare pas */
 	s+=i;   //s=s+i; 
	printf("Suma primelor %d numere naturale este: %d\n",n,s); /* afisam suma primelor n numere naturale */
 
 	getchar();
 	int k;
 	scanf("%d", k);
}
